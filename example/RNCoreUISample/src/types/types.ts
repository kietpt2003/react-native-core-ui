import { SBType } from "storybook/internal/csf";

export const booleanOptional: SBType = {
  name: 'boolean',
  required: false,
};

export const stringOptional: SBType = {
  name: 'string',
  required: false,
};

export const numberOptional: SBType = {
  name: 'number',
  required: false,
};

export const symbolOptional: SBType = {
  name: 'symbol',
  required: false,
};

export const functionOptional: SBType = {
  name: 'function',
  required: false,
};